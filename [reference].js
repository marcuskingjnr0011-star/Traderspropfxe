import { supabaseAdmin } from '../../../_lib/supabase.js';
function allow(res){res.setHeader('Access-Control-Allow-Origin',process.env.ALLOWED_ORIGIN||'*');res.setHeader('Access-Control-Allow-Headers','Content-Type, Authorization');res.setHeader('Access-Control-Allow-Methods','GET, OPTIONS');}
export default async function handler(req,res){allow(res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});const reference=String(req.query.reference||'').trim();if(!reference)return res.status(400).json({error:'Reference is required.'});try{const db=supabaseAdmin();const {data,error}=await db.from('payment_orders').select('reference,email,challenge_id,original_amount,requested_amount,currency,payment_method,status,paystack_status,verified_at,created_at,updated_at').eq('reference',reference).maybeSingle();if(error)throw error;if(!data)return res.status(404).json({error:'Payment order not found.'});return res.status(200).json({ok:true,order:data});}catch(e){return res.status(500).json({error:e.message||'Unable to read payment order.'});}}
